import { h, render, Component } from 'preact';

module.exports = class Work extends Component {
    render(props, state) {
        return <div className="page-container">
            <h4>You gon get this wurk!</h4>
        </div>;
    }
}